import React from 'react';

const SingleUser = (props) => {
    return(
        <main className={'single-user-page'}>
            <h1> This is my pretty single page </h1>
        </main>
    );
}

export default SingleUser;